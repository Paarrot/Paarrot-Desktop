{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for all windows on desktop builds.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "shell:default",
    "notification:default"
  ]
}
